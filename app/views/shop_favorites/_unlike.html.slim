| お気に入り
= form_with model: @shop, url: shop_favorite_path(@shop), html: { method: :delete} do |f|
  = hidden_field_tag :shop_id, @shop.id
  = button_tag id: 'unlike', type: 'submit' do
    i.fas.fa-heart.fa-red
  = @shop.liked_users.count
